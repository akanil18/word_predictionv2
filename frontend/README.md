# Frontend

Open `index.html` in your browser.

- Set the **API base URL** (default `http://127.0.0.1:8000`).
- Type some text and click **Predict**.
- Click a suggested word to append it to your text.

Shortcut: **Ctrl+Enter** predicts.
